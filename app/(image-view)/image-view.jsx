import React from "react";
import { StyleSheet, Text, View } from "react-native";
import { ImageViewContent } from "../../src/components/image-view";
const Gallery = () => {
  return <ImageViewContent />;
};

const styles = StyleSheet.create({});

export default Gallery;
