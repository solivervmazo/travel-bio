import React from "react";
import { StyleSheet, Text, View } from "react-native";
import { GalleryContent } from "../../src/components/gallery";

const Gallery = () => {
  return <GalleryContent />;
};

const styles = StyleSheet.create({});

export default Gallery;
